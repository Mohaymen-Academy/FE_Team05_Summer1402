version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
    - 80:80

  jsonserver:
    restart: always
    build:
      context: ./jsonServer
      dockerfile: Dockerfile