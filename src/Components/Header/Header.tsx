import Navbar from '../Navbar/Navbar';
import IconButton from '../../IconButton';

function Header() {
  return (<header>
      <Navbar/>
      <IconButton/>
    </header>
  )
}

export default Navbar